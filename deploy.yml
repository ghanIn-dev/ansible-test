
---
- name: deploy and restart
  hosts: Target
  become: true
  tasks:
    - name: copy file
      copy:
        src: ~/awx/setup.py
        dest: /usr/share/nginx/html
      notify:
        - restart nginx
  handlers:
    - name: restart nginx
      service:
        name: nginx
        state: restarted
