
---
- name: deploy and restart
  hosts: Target
  become: true
  tasks:
    - name: copy file
      copy:
        src: /var/lib/nginx/file
        dest: /usr/share/nginx/html
      notify:
        - restart nginx
  handlers:
    - name: restart nginx
      service:
        name: nginx
        state: restarted
